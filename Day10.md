# Day 10 - Lists (Fridge Example)

## What I learned
A list is used to store multiple items in one place instead of creating separate variables for each item.

---

# Real Life Example: Fridge

A fridge stores many items together in one place:

- milk 
- eggs 
- vegetables 
- fruits 

Instead of keeping items separately, we store everything inside one fridge.

In programming, this fridge is called a LIST.

---

## Accessing Items in Fridge
Each item inside the fridge has a position (index).
Python
fridge = ["milk", "eggs", "vegetables", "fruits"]

print(fridge[0])  # milk
print(fridge[1])  # eggs
print(fridge[2])  # vegetables
print(fridge[3])  # fruits

## Important Concept
List = fridge (container)
Items inside = things stored in fridge
Index = position of items inside fridge
Index starts from 0

## Real Life Understanding
Opening fridge → accessing list
Picking milk → selecting item using index
Closing fridge → finishing access

## My thought

Now I understand that a list is like a fridge where we store multiple items together, and we can take any item by knowing its position.

